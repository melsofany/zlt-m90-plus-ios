# برومبت تطوير تطبيق iPhone لإدارة ZLT M90 Plus

## الدور

أنت مهندس iOS ومصمم UX/UI خبير. طوّر تطبيق iPhone أصليًا باستخدام **SwiftUI** لإدارة جهاز **ZLT M90 Plus** عبر شبكة Wi‑Fi المحلية. يجب أن يكون التطبيق سريعًا، واضحًا باللغة العربية، داعمًا للاتجاه من اليمين إلى اليسار، وقابلًا للتوسع لاحقًا.

## الهدف الرئيسي

أنشئ تطبيقًا يعرض للمستخدم حالة جهاز ZLT M90 Plus في لوحة واحدة، مع التركيز على المعلومات التالية:

1. نسبة شحن بطارية الجهاز.
2. حالة الجهاز: يعمل على البطارية، يشحن، أو مكتمل الشحن.
3. عدد ساعات العمل المتبقية المتوقعة.
4. تقدير مدة كفاية الشحن الحالي.
5. حالة الاتصال بالإنترنت وشبكة الهاتف.
6. اسم المشغل ونوع الشبكة، مثل 4G أو LTE، إذا كانت البيانات متاحة.
7. قوة الإشارة.
8. نسبة الباقة المستخدمة.
9. حجم الباقة الكلي.
10. حجم البيانات المستخدم.
11. حجم البيانات المتبقي.
12. تاريخ التجديد أو الأيام المتبقية للباقة إذا كان مزود الخدمة أو الجهاز يعرضها.
13. عدد الأجهزة المتصلة بالـ Wi‑Fi.
14. عرض صورة أو رسم واقعي لجهاز ZLT M90 Plus داخل التطبيق.

## متطلبات مهمة للبيانات

لا تعرض أي رقم على أنه حقيقي إذا لم يرجعه جهاز ZLT أو مزود الخدمة. إذا كانت خاصية معينة غير مدعومة في Firmware الجهاز، اعرض الحالة `غير متاحة` مع رسالة واضحة بدلًا من عرض قيمة تقديرية مضللة.

افصل بين البيانات القادمة من الجهاز والبيانات القادمة من مزود الخدمة. بيانات البطارية، حالة الشحن، زمن التشغيل، قوة الإشارة، وعدد الأجهزة المتصلة غالبًا تأتي من واجهة الجهاز المحلية. بيانات الباقة قد لا تكون متوفرة من الجهاز، ولذلك صمّم طبقة مستقلة تسمح بإضافة API لمزود الخدمة أو إدخال البيانات يدويًا.

## المنصة والتقنيات

- اللغة: Swift 5.9 أو أحدث.
- الواجهة: SwiftUI.
- الحد الأدنى: iOS 16.
- النمط المعماري: MVVM مع فصل واضح بين View وViewModel وService.
- الاتصال: URLSession عبر شبكة Wi‑Fi المحلية.
- تخزين رمز الجلسة: Keychain فقط.
- التخزين المحلي للإعدادات غير الحساسة: AppStorage أو UserDefaults.
- لا تسجل كلمات المرور أو رموز الجلسات أو بيانات الباقة في Console.
- أضف `NSLocalNetworkUsageDescription` إلى `Info.plist` مع نص عربي يوضح سبب الوصول إلى الشبكة المحلية.
- ادعم Light Mode وDark Mode.
- استخدم Dynamic Type وإمكانية الوصول VoiceOver.

## الشاشات المطلوبة

### 1. شاشة الترحيب والاتصال

اعرض صورة الجهاز الحقيقية أو رسمًا قريبًا منه في أعلى الشاشة، ثم اسم المنتج `ZLT M90 Plus`، ونصًا يوضح أن الهاتف يجب أن يكون متصلًا بشبكة Wi‑Fi الخاصة بالجهاز.

وفّر الحقول التالية:

- عنوان الجهاز، ويكون افتراضيًا `192.168.0.1` مع إمكانية تعديله.
- اسم المستخدم.
- كلمة المرور.
- زر `اتصال بالجهاز`.
- زر `اكتشاف الجهاز تلقائيًا` إن أمكن.

اعرض حالات الاتصال بوضوح: جاري الاتصال، تم الاتصال، بيانات الدخول غير صحيحة، الجهاز غير موجود، الهاتف غير متصل بشبكة ZLT، أو واجهة الجهاز غير مدعومة.

### 2. لوحة التحكم الرئيسية

صمّم لوحة تحكم عربية RTL مرتبة على شكل بطاقات واضحة. يجب أن تعرض البطاقة الرئيسية صورة الجهاز واسم الجهاز وحالة الاتصال.

#### بطاقة البطارية

اعرض:

- نسبة البطارية مثل `78%`.
- أيقونة بطارية تتغير حسب النسبة.
- حالة الشحن: `يشحن` أو `يعمل على البطارية` أو `مكتمل`.
- الوقت المتبقي المقدر، مثل `متبقٍ تقريبًا 5 ساعات و20 دقيقة`.
- وقت آخر تحديث.

إذا لم يرسل الجهاز نسبة البطارية أو زمن التشغيل، اعرض `غير متاحة من الجهاز` بدلًا من اختراع رقم.

يمكن حساب الوقت المتبقي فقط عند توفر نسبة البطارية ومعدل استهلاك موثوق. يجب توضيح أن الحساب `تقديري`، وتحديثه بناءً على قراءات متعددة لا على قراءة واحدة.

#### بطاقة الإنترنت

اعرض حالة الاتصال باستخدام نص وأيقونة ولون:

- متصل بالإنترنت.
- متصل بالجهاز فقط ولا يوجد إنترنت.
- لا توجد شبكة هاتف.
- جاري الاتصال.
- غير معروف.

اعرض كذلك اسم المشغل ونوع الاتصال وقوة الإشارة. استخدم ألوانًا مناسبة مع عدم الاعتماد على اللون وحده؛ أضف نصًا وأيقونة لكل حالة.

#### بطاقة الباقة

اعرض في شكل دائري أو شريط تقدّم:

- إجمالي الباقة.
- المستخدم.
- المتبقي.
- النسبة المستخدمة.
- تاريخ التجديد.
- الأيام المتبقية.

مثال العرض:

`استخدمت 42% من الباقة`  
`المستخدم: 21 GB من 50 GB`  
`المتبقي: 29 GB`

وضّح مصدر البيانات: `من مزود الخدمة` أو `إدخال يدوي`. إذا لم تتوفر API لمزود الخدمة، أضف زر `إعداد الباقة` يسمح للمستخدم بإدخال حجم الباقة وتاريخ التجديد، مع تسمية البيانات بأنها `تقديرية حسب إدخال المستخدم`.

#### بطاقة الأجهزة المتصلة

اعرض عدد الأجهزة المتصلة حاليًا، مثل `5 أجهزة متصلة`، مع زر لفتح قائمة الأجهزة. تعرض القائمة اسم الجهاز أو عنوان MAC أو عنوان IP إذا كانت البيانات متاحة، وتوضح أن الحظر أو الفصل وظيفة اختيارية لا تنفذ إلا بعد تأكيد المستخدم.

### 3. شاشة تفاصيل البطارية

اعرض النسبة الحالية، حالة الشحن، آخر تحديث، سجلًا بسيطًا لقراءات البطارية، ومتوسط الاستهلاك. يجب أن تفرّق بصريًا بين `الوقت المتبقي الفعلي الذي يرسله الجهاز` و`التقدير المحسوب من التطبيق`.

استخدم العبارة التالية عند الحساب التقديري:

> الوقت المتبقي تقديري وقد يختلف حسب قوة الإشارة وعدد الأجهزة المتصلة واستهلاك البيانات.

### 4. شاشة تفاصيل الاتصال

اعرض:

- حالة الإنترنت.
- اسم المشغل.
- نوع الشبكة.
- قوة الإشارة بوصف نصي ونسبة إن كانت متاحة.
- عنوان IP المحلي.
- مدة الاتصال الحالية.
- آخر وقت نجح فيه اختبار الاتصال.
- زر `تحديث الآن`.

أضف اختبار اتصال اختياريًا إلى عنوان معروف، ولا تعتبر نجاح الوصول إلى صفحة الراوتر دليلًا كافيًا على وجود إنترنت.

### 5. شاشة الباقة والاستهلاك

وفّر إدخالًا يدويًا للباقة عند عدم وجود API رسمية. الحقول هي:

- حجم الباقة.
- وحدة القياس MB أو GB.
- المستخدم حاليًا.
- تاريخ بداية الباقة.
- تاريخ التجديد.
- خيار تحديث البيانات يدويًا.

اعرض رسمًا بيانيًا بسيطًا للاستهلاك اليومي إذا توفرت قراءات تاريخية. لا تدّعِ أن التطبيق يعرف استهلاك الشريحة من الجهاز إلا إذا أعاد الجهاز هذه البيانات فعلًا.

### 6. شاشة صورة الجهاز

اعرض صورة حقيقية عالية الجودة لجهاز ZLT M90 Plus، وليس صورة لجهاز ZTE أو موديل مختلف. ضع الصورة داخل بطاقة تحتوي على اسم الجهاز ومؤشرات مختصرة للبطارية والاتصال.

إذا لم تتوفر صورة مرخصة، استخدم رسمًا توضيحيًا مؤقتًا يحمل وسم `صورة مؤقتة`، واجعل استبداله بصورة المنتج لاحقًا سهلًا من خلال Assets.xcassets. لا تستخدم صورًا عشوائية من الإنترنت ولا توحِ بأنها صورة رسمية للجهاز.

## طبقة الاتصال RouterAPI

أنشئ بروتوكولًا باسم `RouterAPIProtocol` مع تطبيق باسم `ZLTRouterAPI`، ويتضمن وظائف مشابهة لما يلي:

- `login(username:password:)`
- `fetchDeviceInfo()`
- `fetchBatteryStatus()`
- `fetchNetworkStatus()`
- `fetchDataUsage()`
- `fetchConnectedDevices()`
- `updateWiFi(ssid:password:)`
- `restartRouter()`

اجعل كل مسارات API قابلة للتعديل من ملف إعداد واحد، لأن Firmware جهاز ZLT M90 Plus قد يختلف حسب الإصدار أو مزود الخدمة. ادعم JSON وXML إذا لزم الأمر، وأضف سجلات أخطاء آمنة لا تحتوي على بيانات حساسة.

أنشئ نماذج بيانات مستقلة:

- `BatteryStatus`.
- `NetworkStatus`.
- `DataPlanStatus`.
- `ConnectedDevice`.
- `RouterDeviceInfo`.

أضف حقل مصدر البيانات لكل نموذج، مثل `router`, `carrier`, `manual`, أو `estimated`.

## منطق تقدير ساعات التشغيل

نفّذ التقدير فقط إذا توفرت قراءات زمنية كافية. خزّن قراءات النسبة مع وقتها، ثم احسب معدل انخفاض البطارية خلال فترة زمنية. لا تحسب التقدير أثناء الشحن أو عندما يتغير الحمل بشكل كبير.

إذا كانت النسبة الحالية 0% أو لم توجد قراءات كافية، اعرض `لا يمكن التقدير حاليًا`. إذا كان الجهاز يعرض زمنًا فعليًا متبقيًا، فاعرضه أولًا واكتب `حسب الجهاز`. وإذا كان الزمن محسوبًا من التطبيق، اكتب `تقديري` بوضوح.

## حالات الخطأ والبيانات غير المتاحة

صمّم حالات UI للسيناريوهات التالية:

- الهاتف غير متصل بشبكة ZLT.
- عنوان IP خاطئ.
- فشل تسجيل الدخول.
- انتهاء الجلسة.
- الجهاز لا يدعم قراءة البطارية.
- الجهاز لا يعرض بيانات الباقة.
- لا توجد تغطية هاتف.
- انقطاع الإنترنت مع استمرار الوصول إلى الراوتر.
- استجابة غير مفهومة من Firmware.
- فشل مؤقت مع زر إعادة المحاولة.

استخدم رسائل عربية بسيطة، ولا تعرض أخطاء تقنية طويلة للمستخدم العادي. أضف زر `تفاصيل تقنية` اختياريًا للمطور.

## الأمان والخصوصية

لا تحفظ كلمة مرور الراوتر في UserDefaults. استخدم Keychain. لا ترسل بيانات الجهاز إلى خادم خارجي دون موافقة واضحة. اجعل كل الاتصالات محلية افتراضيًا. إذا أضيف تكامل مع مزود الخدمة، اعرض سياسة الخصوصية ومصدر البيانات وموافقة المستخدم.

أضف تأكيدًا قبل تنفيذ أي عملية مؤثرة، مثل تغيير كلمة مرور Wi‑Fi أو إعادة تشغيل الجهاز. لا تنفذ هذه العمليات في النسخة التجريبية قبل ربط مسارات Firmware والتحقق منها.

## الاختبارات المطلوبة

اكتب اختبارات وحدة للنماذج وحساب نسبة الباقة والوقت المتبقي. اكتب اختبارات لحالات البطارية غير المتاحة، الشحن، والقراءات المتناقصة. أضف Mock API يسمح بتشغيل التطبيق دون وجود جهاز فعلي.

اختبر الواجهات على iPhone صغير وكبير، وفي الوضعين الفاتح والداكن، ومع تكبير النص، واتجاه RTL. لا تعتبر التطبيق جاهزًا للإنتاج قبل اختباره على جهاز ZLT M90 Plus فعلي وبإصدارات Firmware مختلفة.

## المطلوب تسليمه

سلّم مشروع SwiftUI كاملًا ومنظمًا، يتضمن:

1. جميع الشاشات المذكورة.
2. نماذج البيانات وViewModels.
3. RouterAPI قابلة لتعديل المسارات.
4. Mock API للعرض والتطوير.
5. Keychain Service.
6. Assets منفصلة لصورة الجهاز.
7. إعدادات `Info.plist` المطلوبة.
8. اختبارات وحدة.
9. README يشرح تشغيل المشروع وربطه بجهاز حقيقي.
10. توضيح صريح لكل ميزة تعتمد على دعم Firmware أو مزود الخدمة.

لا تستخدم بيانات وهمية في شاشة الإنتاج على أنها بيانات حقيقية. استخدم البيانات التجريبية فقط عند تفعيل وضع Demo واضح للمستخدم.
